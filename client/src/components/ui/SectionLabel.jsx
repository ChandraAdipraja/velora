const SectionLabel = ({ children }) => {
  return (
    <p className="mb-3 text-[11px] font-semibold uppercase tracking-[0.35em] text-[var(--champagne)]">
      {children}
    </p>
  );
};

export default SectionLabel;
